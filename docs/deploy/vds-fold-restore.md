# VDS: свернуть в образ и развернуть обратно

Как на время простоя (отпуск, пауза в разработке) **свернуть прод-VDS в образ и удалить
сервер**, чтобы не платить ~1800 ₽/мес за простаивающую машину, и потом **быстро
развернуть обратно на тот же IP без правки DNS**.

Выключенный VDS у Timeweb тарифицируется полностью (ресурсы зарезервированы) — экономит
только удаление. Смежное: провижн с нуля и туннель — `.claude/ralph/provision/README.md`;
бэкап прод-БД — `docs/deploy/backup.md`.

## Ключевая идея: IP плавающий — его НЕ удаляем

Прод-адрес `186.246.7.204` — это **floating IP** (отдельный ресурс, id
`ef646603-254e-44d9-aca9-4eb6f2592922`), а не IP, приклеенный к серверу. DNS
`pixeltanks.ru` (A-запись) смотрит именно на него. Поэтому:

- удаляем сервер — floating IP **остаётся** за аккаунтом;
- при восстановлении просто **пере-привязываем** его к новому серверу (`bind_floating_ip`);
- **DNS не трогаем вообще** — адрес тот же.

Цена за это — 180 ₽/мес за сам floating IP в простое. Против экономии 1800 ₽/мес на
сервере — берём, ради вечного адреса и нуля возни с DNS.

> Живём на **IPv4**. В DNS есть AAAA-запись (IPv6) — floating IP её не покрывает, при
> пересоздании сервера IPv6 будет другой. **AAAA удалить** (см. шаг сворачивания) — сайт
> IPv4-only, restore тогда нулевой по DNS. MX/TXT(SPF) для почты не трогаем.

## Постоянные факты (server id меняется, остальное — нет)

| Что               | Значение                                                                                  |
| ----------------- | ----------------------------------------------------------------------------------------- |
| Сервер            | `ralph-prod`, тариф Cloud MSK 80 — 4CPU / 8GB / 80GB, Ubuntu 24.04                        |
| Локация / зона    | `ru-3` (Москва) / `msk-1`, `preset_id=4803`, project `46271`                              |
| Floating IP       | `186.246.7.204`, id `ef646603-254e-44d9-aca9-4eb6f2592922`                                |
| Домен             | `pixeltanks.ru`, A → floating IP                                                          |
| Текущий server id | меняется при каждом пересоздании — брать из `list_servers` / вывода `create_server`       |
| image_id          | свой на каждое сворачивание — писать в `.claude/state/HANDOFF.md` и память, не хардкодить |

## Стоимость в простое

| Ресурс                           | ₽/мес                 |
| -------------------------------- | --------------------- |
| Floating IP (держим ради адреса) | 180                   |
| Образ (хранение диска)           | ~20                   |
| S3 `pelmenya`                    | 1                     |
| **Итого простой**                | **~200** вместо ~2000 |

## Свернуть

1. **Остановить ralph** (если крутится) и убить мониторы — чтобы образ снялся с тихого диска:
    ```bash
    pkill -f "ralph.js|monitor.js"      # проверить: pgrep -af "ralph.js|monitor.js"
    ```
2. (Опц.) Свежий дамп прод-БД для offsite-спокойствия: `sudo /opt/pixel-tanks/scripts/backup-db.sh`.
   Образ и так хранит `payload.db` целиком — это подстраховка.
3. **Снять свежий образ** — Timeweb MCP `create_image(server_id=<текущий>)` (billable, ~20 ₽/мес).
   Дождаться `status=created` через `get_image` / `list_images` (`progress=100`).
   Старый образ прошлого сервера можно удалить, чтобы не платить дважды.
4. **Записать новый `image_id`** в `.claude/state/HANDOFF.md` и память проекта.
5. **Удалить AAAA-запись** `pixeltanks.ru` (IPv6) — панель «Домены» или `delete_dns_record`.
6. **Удалить сервер** — **только через панель** https://timeweb.cloud/my/servers.
   MCP `delete_server` не поддерживается.
7. **НЕ трогать** floating IP и образ — они и держат быстрый restore.

## Развернуть

1. `create_server` из образа: `image_id=<из HANDOFF>` (вместо `os_id`), тот же
   `preset_id=4803`, зона `msk-1`, `ssh_keys_ids`, `is_backups=false`. ОС и окружение — из образа.
2. `bind_floating_ip(ip_id=ef646603-…, resource_id=<новый server id>, resource_type=server)`.
   **DNS не трогаем** — `186.246.7.204` уже в A-записи.
3. SSH на сервер (тот же адрес). Туннель (`ss-local`/`privoxy`, `Restart=always`) и env
   поднимаются сами — проверить:
    ```bash
    curl -x http://127.0.0.1:8118 https://api.ipify.org   # egress вне РФ (Франкфурт)
    claude -p 'Ответь одним словом: OK'                    # → OK через туннель
    curl -s -o /dev/null -w '%{http_code}\n' https://pixeltanks.ru   # 200
    ```
    Если токены протухли за простой: `CLAUDE_CODE_OAUTH_TOKEN` — через
    `.claude/ralph/provision/update-token.sh`, `GH_TOKEN` — руками в `ralph.env`
    (см. provision README, «Ротация headless-токена»).
4. **Запустить ralph** по RUNBOOK — перезапуск заодно активирует непрерывный prod
   (флаг `haltBeforeDeploy:false` уже в main, читается на старте).

## Проверить, что развернулось верно

- `list_servers` → сервер `on`, `public_ipv4=186.246.7.204`.
- `list_floating_ips` → `is_bound=true`, `bound_to.resource_id` = новый server id.
- `https://pixeltanks.ru` → 200; ralph-панель (`monitor.js`) зелёная.
