# Session Handoff

**Дата**: 2026-07-29

## Текущая задача

**Трек «Фреймворк ralph» пройден полностью**: 6 фаз, 24 issue, 6 релизов, PR #384/#385/
#389/#391/#392/#393. `ralph.js` — с 4352 строк до **119** (тонкий entry поверх 14 TS-модулей,
41 тестовый файл). Раннер остановился штатно («все фазы завершены») 29.07 в 09:08.
Прогон занял 26.5 ч календарных, вмешательство человека — одно (см. ниже).

## Что важно знать про результат

- **Другие провайдеры НЕ подключены в бою.** Фаза 6 построила рантаймы Kimi/OpenAI как
  реализации интерфейса (внутри `adapters-impl.ts`/`orchestrator.ts`/`config-profile.ts`),
  плюс `AGENTS.md` + детерминированный чек дрейфа `scripts/agents-md-drift.mjs`. Но ключей
  провайдеров в `/root/ralph.env` нет, `ralph.config.json` маршрутизирует всё на Claude,
  ни одна сессия на не-Claude модели не запускалась. Проверка — фаза 7 (#379).
- **Распил в бою ещё не работал.** Весь прогон шёл на коде, загруженном при старте в 06:30
  (до фазы 1) — следствие `haltBeforeDeploy: false`. Первый боевой прогон нового ядра будет
  при следующем запуске: смотреть внимательно на первые итерации.
- **Лимит Anthropic бил 5 раз, суммарно >11 ч ожидания против ~8 ч работы.** Худший режим —
  `fable-5` на `complexity:expert` (#365: 3 захода, 6.5 ч простоя при ~40 мин работы).
  Тюнинг роутинга: объёмные рефакторинги → `complexity:high` (opus), `expert`/fable беречь
  для задач, где цена ошибки в корректности. Иронично, что #376 (кросс-провайдерные
  фолбеки) эту боль и лечит — но заработает только со следующего запуска.
- **Автоснятие `blocked` отработало вживую впервые** (фаза 6, 3 блокера — единственные за
  трек): починка → повторное ревью моделью не слабее (fable, фолбэк тоже поднят до fable) →
  метка снята за 2 круга из 3, без человека.
- **Единственное вмешательство:** 28.07 сессия по #204 упала с exit 1, оставив 541 строку
  незакоммиченной; раннер встал на «грязное дерево». Человек прогнал тесты раннера (зелёные)
  и закоммитил остаток — следующая сессия дописала. Откат стоил бы 36 мин работы и квоты.

## Следующие шаги

1. **Разложить `.claude/ralph/` по папкам** (решение пользователя 29.07): сейчас 60 файлов
   в плоской директории вперемешку — модули, тесты, служебные (`ralph.log`/`state`/`lock`/
   `monitor.out`), конфиги, доки. Предложенная раскладка: `core/` (orchestrator, gate,
   review, deploy-check, api-limit, state-lock, worktree, exec), `adapters/`, `shared/`
   (ralph-util, side-effect-guard, config-profile), `runtime/` (ralph.js-entry, monitor,
   deadman, telegram-notifier), `docs/`; конфиг и гитигноренные — в корне. **Радиус шире,
   чем кажется:** импорты в 41 тестовом файле, `package.json` (`typecheck:ralph`, vitest-
   проект `ralph`), `gate-env.js` (читает allowlist по `__dirname`), пути в RUNBOOK/CLAUDE.md,
   `.gitignore`, `review.escalateOnPaths`. Завести issue `complexity:medium` + `area:devops` и
   **пустить раннер на нём одном при человеке** — это же первая боевая проверка распила.
2. **Фаза 7 — живая валидация переносимости (#379)**, в очередь раннера не заряжать
   ([ЧЕЛОВЕК]). Она же — первая реальная проверка не-Claude рантайма.
3. Тюнинг `modelRouting` (см. выше) — отдельной веткой, конфиг раннера не править на ходу.
4. Находки прогона в очередь: **#386** (dry-run кодер-сессий пишет в общий `ralph.log` и
   ослепляет deadman; за прогон воспроизвелось 8+ раз), **#387** (после мерджа остаётся
   локальный ref ветки фазы, текст предупреждения врёт про «дерево человека»), **#390**
   (падение сессии диагностируется окольно, stdout сессии теряется).
5. Хвосты: Playwright-smoke Select на `/design-system`; 3 major из ревью #355.

## Open questions

- apiLimit раздут под тест долгого лимита (16×6ч, память) — когда откатывать?
- Фиксировать ли находки #386/#387/#390 в журнале как `found-after` по фазам 2–4?
