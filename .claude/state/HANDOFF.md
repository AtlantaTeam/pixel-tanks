# Session Handoff

**Дата**: 2026-07-24

## Текущая задача

**Непрерывный prod включён и смёржен** (#258): `haltBeforeDeploy:false` в main, активируется
при следующем запуске ralph (флаг читается на старте). Раннер сейчас **остановлен**.
Ближайшее по плану — на следующей неделе **свернуть прод-VDS в образ на время отпуска**
(runbook готов: `docs/deploy/vds-fold-restore.md`, #259 смёржен). Дерево чистое, `main` в синке.

## Последние принятые решения

- **Сворачивание VDS: floating IP НЕ удаляем.** Прод-адрес `186.246.7.204` — floating IP
  (id `ef646603-…`), DNS смотрит на него. Держим за 180 ₽/мес → restore без правки DNS
  (пере-привязка `bind_floating_ip`). **Живём на IPv4 → AAAA-запись удалить.** Простой
  ~200 ₽/мес вместо ~2000. Старый образ в аккаунте — от ПРОШЛОГО сервера (8637397), снимать
  СВЕЖИЙ образ текущего (8638987). Поправлен противоречащий совет в provision/README.
- **Continuous prod активируется при restore** после отпуска — перезапуск ralph = включение,
  два дела совпадают.
- **Бэклог-вектор #261** (Telegram-канал подтверждений моих действий через `PreToolUse`-хуки,
  approval-gate; ralph НЕ гейтить — он AFK). Скелет под `prd` готов.
- **Бэклог-вектор #262** (переносимость ralph в др. проекты). **Паттерн НЕ зафиксирован** —
  «адаптеры» лишь стартовая гипотеза, можем взять другой (см. коммент в #262). Связан с #204
  (де-хардкод значений в конфиг — пролог).

## Следующие шаги

1. **Перед отъездом (по runbook):** снять свежий `create_image(8638987)` → записать image_id
   СЮДА → удалить AAAA `pixeltanks.ru` → удалить сервер через панель. IP и образ не трогать.
2. **По возвращении:** `create_server` из образа → `bind_floating_ip` → запуск ralph
   (=включит непрерывный prod). DNS не трогаем.
3. Проработать #261 и #262 через `prd`, когда дойдут руки (у #262 — сначала выбрать паттерн).

## Open questions

- #262: какой паттерн переносимости брать (адаптеры vs иное) — решаем на проработке.
- Ложный DEADMAN 23.07: standalone дашборд-монитор не гаснет после чистого финиша раннера
  (лог замолкает, хвост 🏁/👁 не классифицируется как `stopped` → default-порог 5м). Кандидат
  в issue либо просто не поднимать дашборд-монитор вручную.
- Три старые ветки `docs/ralph-*` (не из этой сессии) — подчистить?
