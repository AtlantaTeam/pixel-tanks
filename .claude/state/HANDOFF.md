# Session Handoff

**Дата**: 2026-07-30

## Текущая задача

**Дизайн боевого экрана (#280) закрыт, раннер заряжен, но НЕ запущен** — упёрлись в лимиты,
31.07 занят другой работой. Запуск — первым делом в новой сессии: tmux по RUNBOOK, профиль
`prod`, `--reset`. Подготовка сделана целиком, доделывать нечего. Трек «Экран боя ralph»:
4 фазы, issues #421–#427, все на доске.

## Последние принятые решения

- **PRD для трека сознательно НЕ пишем.** Хендофф Claude Design (`docs/design-system-theming/
game-screen/handoff.md`, 27 КБ) — спека выше типового PRD: layout по слоям, точные значения,
  все состояния, модель состояния стора, раздел «что делать в кодовой базе». PRD поверх стал бы
  пересказом, который разъедется с оригиналом.
- **Спека обязана быть в `origin/main` до старта** (PR #419): раннер детачит дерево на
  origin/main, локальных файлов не видит — иначе issues ссылались бы в пустоту.
- **Вывод Claude Design ревьюим рендером и замером, не глазами.** Оба блокера трёх ревизий были
  невидимы на скриншоте: телеметрия поверх танков (полоса 196–301px против танков на 270px) и
  переполнение планшета (`right=926` при `scrollWidth:768`). README хендоффа при этом утверждал
  обратное — верить надо файлу, а не спеке.
- **Геймплейные конфликты называем сами.** Полная парабола-подсказка и точное число ветра
  формально соответствовали брифу, но отменяли пристрелку из GDD. Итог: сегмент ~12% без точки
  падения; ветер — стрелка + грубая сила, точное значение после первого выстрела (`windRevealed`).
- **Фаза 1 — `complexity:high` по риску, не по объёму:** `playerPoints`/`enemyPoints` живут в
  8 файлах (46 вхождений), включая HUD реплея и тест детерминизма, плюс коллекция `scores`.
- Правило про скилл `ui-ux-pro-max` **уже было** в `ralph.project.md` — вместо выдуманной строки
  поправлено реально устаревшее: вьюпорты 375×667 → канон 390/768/1280/1920 + замер
  переполнения как обязательный шаг (PR #420).

## Следующие шаги

1. **Запустить раннер** на трек «Экран боя» (`prod`, `--reset`). `haltBeforeDeploy: false` —
   пройдёт все 4 фазы за прогон. Помнить: конфиг читается ОДИН раз при старте, дозарядить
   фазу мерджем во время прогона нельзя.
2. После закрытия #280 — удалить транспортную ветку `assets/issue-280` (6 МБ кадров макета);
   картинки в комментариях issue тогда станут битыми, решения записаны текстом.
3. Дальше по milestone «Экраны игры · дизайн»: #281–#286 тем же циклом (бриф → рендер →
   замер → правки).

## Open questions

- Скилл `ui-ux-pro-max` пользователь хочет обновить — неизвестен источник новой версии
  (репо автора / npm), спросить при следующем заходе.
- Сузить `review.escalateOnPaths` — исключить `**/*.md`? Правка докблока эскалирует ревью до
  fable; в прошлом прогоне это дважды дало ~100 мин ожидания на API-лимите.
