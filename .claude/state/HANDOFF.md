# Session Handoff

**Дата**: 2026-07-18

## Текущая задача

Фаза 3 (тач «оттяни и отпусти») завершена, ревью fable пройдено, 2 минорных
бага из ревью пофикшены (коммит 840d61c), **PR #44 смерджен squash в main**,
milestone Фазы 3 закрыт. Локальный репо на свежем main (f39af0e). Ralph
`state = {count:0, phaseIndex:1}` — указывает на **Фазу 4**. Всё готово к
рестарту loop на Фазу 4. **СЛЕДУЮЩИЙ ШАГ: запустить `node .claude/ralph/ralph.js`
(без `--once`, фоново)** — loop уйдёт на milestone «Фаза 4: Клавиатурная схема»
(3 открытых issue: #13 medium, #14 low, #15 high).

## Последние принятые решения

- **AFK-вердикт**: внутри фазы — полностью AFK (issues → PR → ревью); между
  фазами один HITL-гейт — мердж PR делает Дима (он санкционировал мердж #44).
  Авто-мердж НЕ делаем: фазы зависимы, ревью может вернуть работу.
- Прошлый прогон после Фазы 3 сам перескочил на Фазу 4 и упёрся в circuit
  breaker (10 итераций claude падали за секунды — упор в лимит + Фаза 4 строилась
  бы поверх несмерженного main). Урок: рестарт делать ТОЛЬКО после мерджа PR фазы.
- Perf-долг из ревью Фазы 3 вынесен в issue **#45** (Фаза 6): offscreen-террейн
    - пул точек прицела + токены цвета движка (сейчас `#ffcd75` захардкожен).
- E2e-долг (тест бьёт по React-обработчикам, не по браузерному тач-пайплайну) —
  на будущее CDP `Input.dispatchTouchEvent`, issue не заводили.
- Ручные гейты (npm install на ask): агент вешает label `blocked` + коммент,
  раннер пропускает; label снимает человек.
- Milestone закрывается свипом раннера, когда issues разобраны И PR смерджен.
- Ревьюер постит через `gh api repos/...` (в allow, settings.json).
- Сквош-мердж: ветка после мерджа расходится с main — новые инфра-коммиты
  возить отдельной веткой от main, не пушить в смерженную.
- E2e: NO_PROXY для localhost в playwright.config.ts (VPN-прокси 127.0.0.1:10810
  ронял webServer 502). Зомби next dev на 3050 убивать через netstat/Stop-Process.
- Роутинг моделей: medium→sonnet, high→opus, expert→fable.

## Следующие шаги

1. Запустить AFK Фазы 4: `node .claude/ralph/ralph.js` (фоново). Следить по
   ralph.log; после фазы — показать Диме PR + ревью, он мерджит.
2. После мерджа Фазы 4 — снова: мердж → рестарт на следующую фазу.
3. Финальный гейт после Фазы 9 — предложить /code-review ultra по всему game-next.

## Open questions

- Мерджить PR фазы может только Дима — пинговать его.
- OPENAI_API_KEY для генерации артов — Дима положит в .env.local по запросу.
