# Session Handoff

**Дата**: 2026-07-24

## Текущая задача

**Трек дизайн-консистентности.** Фича `design-system-theming`: PRD+план готовы, заведён
**milestone #38** (агентские фазы #270–273) + **#269 [ЧЕЛОВЕК]** (Фаза 1). Пользователь
**сейчас идёт в Claude Design** делать #269 → вывести палитру/шрифты/темы из hero-арта в
`docs/design-system-theming/token-spec.md`. Он **блокирует** #270–273; ралф на них НЕ в
очереди, пока нет token-spec. Всё в main, дерево чистое.

## Последние принятые решения

- **Дизайн = два слоя**: консистентность держит **код-нативный барьер** (poupe-lint «только
  токены» + Playwright-визрегрессия), НЕ GUI-редактор (вывод deep-research, `docs/design-system-tooling-research/`). **Claude Design выбран** как человеко-инструмент генерации/вкуса — дополняет барьер.
- **ИИ не судит воспринимаемую красоту** → человеческий гейт вкуса; визрегрессия «вмораживает»
  его вердикт в baseline (судит раз — машина энфорсит). Отсюда деление фаз человек/агент.
- **PRD темизации даёт Claude Design свободу** (шрифты/палитра/эффекты/темы не зафиксированы;
  жёстко только кириллица, WCAG, единый источник `@theme`). Hero-арт неоновее нынешнего Pico-8.
- Порядок: `design-system-theming` → потом барьер `design-consistency-gate` (PRD готов, plan-phase позже).
- **Правило: актуальный HANDOFF обязан быть в main** (через PR, main защищён) — закреплено в скилле handoff + CLAUDE.md.
- **VDS-отпуск в силе**: floating IP `186.246.7.204` НЕ удалять (restore без правки DNS), IPv4-only → AAAA убрать, снимать СВЕЖИЙ образ 8638987. Runbook `docs/deploy/vds-fold-restore.md`.

## Следующие шаги

1. Человек: #269 в Claude Design → `token-spec.md`. Потом **смотрим дизайн вместе через
   Playwright**, я кодифицирую в `@theme`, и ставим ралф на #270–273.
2. `plan-phase` по барьеру `design-consistency-gate` (можно параллельно — зависит от факта токенов, не от вкуса).
3. Перед отпуском — свернуть VDS по runbook (образ + держим floating IP), image_id записать сюда.

## Open questions

- Ресёрч (`docs/design-system-tooling-research/`): #2 Open Design (нужны URL), #3 РФ-доступность, #4 `@theme`↔DTCG, #5 вендоринг `ui-ux-pro-max`.
- #262: паттерн переносимости не выбран. Баги #263–265 смёржены, но визуально «не всё» — оставлено.
- Ложный DEADMAN: standalone дашборд-монитор не гаснет после финиша — не поднимать вручную либо issue.
