# Session Handoff

**Дата**: 2026-07-28

## Текущая задача

Заведён трек **«Фреймворк ralph»**: рефакторинг раннера в переносимый мини-фреймворк.
PRD+план — `docs/ralph-mini-framework/` (PR #380), бэклог — 7 milestone'ов, 27 issues
(#358–#379 + привязанные #232/#145/#252/#204/#262), очередь раннера — фазы 1–6
(PR #382). Всё в `origin/main`. Раннер НЕ запущен — запуск за человеком по RUNBOOK.

## Последние принятые решения

- **TS в ядре — только нативный type stripping Node 24** (локально и VDS — Node 24),
  erasable-only, без билд-шага: сохраняем «раннер работает до npm ci».
- **Отдельный репо/npm-пакет — нет** до второго проекта-потребителя; ядро зреет в репо.
- **Фаза 7 не в очереди раннера**: там [ЧЕЛОВЕК]-валидация (#379, живой `--once`);
  завести в конфиг после фазы 6.
- **#204 не распилен** на мелкие issues (просьба «не дублировать») — ему дописан
  чек-лист в body; так же дополнены #232 (tsconfig+TS) и #262 (рантайм кодер-сессии).
- **Кросс-провайдерные фолбеки** (лимит → другой провайдер; провал гейта → сильнее
  модель) дописаны в #376. **Панель судей + метрики моделей по журналу** — #381,
  backlog без milestone, кандидат на трек после фреймворка.
- Мид-скоуп от пользователя: фаза 6 = рантаймы **Kimi + OpenAI + AGENTS.md**
  (маршрут endpoint-vs-CLI решает research-issue #372 в начале фазы).

## Следующие шаги

1. Запустить раннер на «Фреймворк ralph · Фаза 1» (tmux, RUNBOOK) — человек.
2. Перед/параллельно: Playwright-smoke Select на `/design-system` (хвост прошлой
   сессии — исходная жалоба «попап не открывается» так и не проверена вживую).
3. Разобрать 3 major из ревью #355 по `.claude/ralph/review-findings.jsonl`.

## Open questions

- Ревью-эскалация уже кроет `.claude/ralph/**` (fable) — достаточно ли для распила
  боевой машины или первые PR фазы 1 смотрим глазами?
- apiLimit раздут под тест долгого лимита (16×6ч, память) — когда откатывать?
