# Session Handoff

**Дата**: 2026-07-27

## Текущая задача

Раннер (**prod, непрерывный**) добивает баг-фиксы **#47** «Экран боя и легаси UI»:
**#337 закрыт** (снимок очков на конце боя), сейчас **#338** («В меню», haiku) → **#339**
(выпилить легаси `pixel-border`). Фаза 7 верстки смерджена (#345). Дашборд в tmux `work:3`,
раннер в `work:2`, монитор+deadman живы.

## Последние принятые решения

- **Скилл `ui-ux-pro-max` — В РЕПО, не глобально** (PR #344): `.agents/skills/` + симлинк
  `.claude/skills/` + `skills-lock.json`, `ralph.md` путь переведён на проектно-относительный.
  Почему: глобальный `~/.claude/skills/` не переживает restore VDS. Скилл офлайн, безопасен.
- **Скилл фактически НЕ применялся** в фазах 1–6 (наказ ссылался на несуществующий глобальный
  путь → молча падал) и в фазе 7 (ветку отрезали ДО #344 — скилла в дереве не было). Правильно
  wired только на ветке #47 (создана после #344) → баг-фиксы #338/#339 идут со скиллом.
- **Заведён #346** (`area:devops`/`complexity:high`/`backlog`, на доске): барьер — детерминированно
  верифицировать вызов скилла (инвариант №5 «барьеры важнее промптов»). Трогает гейт → человеку,
  не autopilot. Раннер его не возьмёт (backlog без milestone).

## Следующие шаги

1. Утром: как **#339** закроется и #47 добьётся — вместе глянуть диффы всех 3 фиксов +
   поднять game-over экран (договорённость с Димой), оценить, что дал скилл по результату.
2. Обсудить реализацию барьера **#346** (с человеком, т.к. гейт).

## Open questions

- Барьер #346 — какой из 3 кандидатов (присутствие / лог `search.py` / гейт-проверка) делаем?
- Недельный (долгий) лимит: `parseResetWaitMs` понимает только сброс внутри суток — чинить или нет.
