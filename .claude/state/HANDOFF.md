# Session Handoff

> Обновляется автоматически перед закрытием сессии. Перезаписывается целиком, максимум 50 строк.

**Дата**: 2026-07-18

## Текущая задача

Внедрение методологии разработки (boilerplate из claude-code-advanced-course) + подготовка к фазе 6 (auth). Прогресс фаз — таблица в `web/CLAUDE.md` (фазы 1–5 done).

## Последние принятые решения

- Стек подтверждён после критического ревью; единственная замена — **DaisyUI выпиливаем** → своя мини-библиотека `shared/ui` с игровой темой (пиксель-шрифт, NES-рамки). Использований DaisyUI в коде мало (4 btn, 1 card) — миграция дешёвая.
- Методология: boilerplate установлен (хуки guard/format/load-context/backup, HANDOFF, скиллы prd/plan-phase/research). `/issues` и Ralph — ОТЛОЖЕНЫ до переезда в новый репо (конфликт с правилом «без коммитов»).
- Стековые скиллы: vercel-react-best-practices, vercel-composition-patterns, web-design-guidelines, payload (официальный). Свои: playtest, game-feel.
- Codex CLI (standalone 0.144.5, Free-тариф) — дешёвое второе мнение через `codex exec`.
- Арты: генерит пользователь в ChatGPT UI → PNG в проект; API отдельно платный, пока не берём.

## Следующие шаги

1. Выпилить DaisyUI → `shared/ui` (кнопка/диалог/таблица, Press Start 2P, NES-рамки), проверить Playwright
2. Фаза 6 (auth) по новой методологии: `/prd` → `/plan-phase` → `/research` → код
3. После auth — решить про переезд в новый репо + включение Ralph на фазы 7–9

## Open questions

- Когда переезжаем в новый репо (предложение: после фазы 6)
- Нужен ли Radix для диалогов или хватит своих компонентов
