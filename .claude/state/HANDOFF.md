# Session Handoff

**Дата**: 2026-07-21

## Текущая задача

Прод-режим ralph. Трек бэкапов БД закрыт полностью (#119/#120/#121 в Done, PR #126
смерджен). Эта сессия ушла в побочный баг — ежеутренний релогин claude на VDS;
починен и задокументирован (`bb8bf67`), кода фаз не трогали.

Следующее — **Фаза 2 (#71–75)**, старт с **#71** (схема профилей playground/prod).
К реализации не приступали: нужен разбор текущего `ralph.config.json` + как его
читает `ralph.js`, чтобы профили легли на существующую загрузку, а не рядом.

## Последние принятые решения

- **Ежеутренний релогин claude устранён.** `.bashrc` экспортировал статичный
  `CLAUDE_CODE_OAUTH_TOKEN` из `ralph.env`, а он перебивает `~/.claude/.credentials.json` →
  логин чинил файл, который не использовался. Фикс: `unset` + алиас `ralph-env`.
  **`.bashrc` вне git** — при чистом провижне вносить руками (как unit-файлы бэкапа).
  **Перед запуском ralph обязателен `ralph-env`**, иначе headless без токена.
  Механизм и ротация: `.claude/ralph/provision/README.md` → «Аутентификация».
- **Токен НЕ ротировали** — Дима отложил осознанно (доступ к серваку только у него).
  Инструмент готов: `provision/update-token.sh`. Отзыв старого — вручную на claude.ai.
- **Доска: «автоматизация Done не срабатывает» под сомнением** — #119/#120/#121
  оказались в Done сами; проверить на следующем закрытии.
- **Claude/ralph живут в tmux `work`**; ритуал `w` → `claude --continue` (переживает
  обрыв SSH, ПРОВЕРЕНО), но НЕ reboot VDS; авто-воскрешение отложено в #103.
  Дока: `docs/ralph-prod-mode/vds-sessions.md`. timeweb MCP подключён (VDS id 8638987).
- **DI-граница runLoop/preflight**: дефолтные коллабораторы читают ГЛОБАЛЬНЫЙ config
  (в проде config===cfg, ок); независимый DI — вне scope, долга НЕ заводили.
- **Бэкап прод-БД**: systemd timer enabled на VDS (00:03 UTC), дока `docs/deploy/backup.md`.
  Restore проверен на data-level; app-level — когда прод накопит записи (`payload.db` пуст).
  Unit-файлы вне git (`/etc/systemd/system`) — правки в репо ставить руками.

## Следующие шаги

1. **#71** — профили в `ralph.config.json`: наследование общих полей, `prod` — только
   дельта, невалидный/отсутствующий профиль → fail-closed. Сначала прочитать
   `ralph.config.json` + загрузку конфига в `ralph.js`. Дальше Фаза 2 — #72–75.
2. #103 (backlog): штатный запуск ralph без ручного tmux + авто-воскрешение сессии.
3. Offsite-бэкап БД (S3 Timeweb) — вектор на будущее, сейчас бэкап на диск того же VDS.

## Open questions

- Свернуть ли прод VDS (~2001₽/мес)? Golden-образ + удаление VDS+IP → ~21₽/мес.
  Необратимо — только по команде Димы.
- Ротировать ли `GH_TOKEN` и ss-пароль (прошли через чат)? По `CLAUDE_CODE_OAUTH_TOKEN`
  решение принято — отложено.
