# Session Handoff

**Дата**: 2026-07-28

## Текущая задача

**Очередь раннера ПУСТА — петля прошла все 8 фаз и чисто завершилась** (`🏁 Ralph loop
завершён`, 27.07 20:37). Верстка ДС фазы 1–7 + баг-фиксы #47 — всё смерджено (последний
#347). Раннер **не работает**, простаивает (это норма, не крэш). В tmux `work:3` крутится
интерактивный дашборд (безобидно), `work:2` — раннер завершён. Прод на `b37739b`.

## Последние принятые решения

- **Скилл `ui-ux-pro-max` — В РЕПО, не глобально** (PR #344): `.agents/skills/` + симлинк
  `.claude/skills/` + `skills-lock.json`, `ralph.md` путь проектно-относительный. Почему:
  глобальный `~/.claude/skills/` не переживает restore VDS. Скилл офлайн, безопасен.
- **Скилл фактически НЕ применялся** в фазах 1–6 (наказ ссылался на несуществующий глобальный
  путь → молча падал) и в фазе 7 (ветку отрезали ДО #344). Правильно wired только на ветке
  #47 → баг-фиксы #338/#339 МОГЛИ его использовать (факт вызова не логируется — см. #346).
- **Заведён #346** (`area:devops`/`complexity:high`/`backlog`, на доске): барьер —
  детерминированно верифицировать вызов скилла (инвариант №5 «барьеры важнее промптов`).
  Трогает гейт → человеку, не autopilot. Раннер не возьмёт (backlog без milestone).

## Следующие шаги

1. Ревью результатов #47: диффы #337/#338/#339 + поднять game-over экран (договорённость с
   Димой), оценить по результату, что дал скилл на #338/#339.
2. Решить, что раннеру дальше (очередь пуста): следующий трек/фазы или пауза.
3. Обсудить реализацию барьера **#346** (с человеком, т.к. гейт).

## Open questions

- Барьер #346 — какой из 3 кандидатов (присутствие / лог `search.py` / гейт-проверка) делаем?
- Мелочь-хвост: в #344 случайно затрекались `.agents/**/__pycache__/*.pyc` — почистить (gitignore + untrack).
- Недельный (долгий) лимит: `parseResetWaitMs` понимает только сброс внутри суток — чинить или нет.
