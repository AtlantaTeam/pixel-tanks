# Session Handoff

**Дата**: 2026-07-24

## Текущая задача

**Дизайн-трек, два потока параллельно.** (1) **Ралф кодифицирует дизайн-систему** —
milestone #38 «Дизайн-система и темизация из hero-арта»: #270-272 ✅, идёт #273 (темизация+
контраст), осталась витрина **#290** (`/design-system` роут). Раннер в tmux `work:2`,
дашборд-монитор в `work:3` (**ГАСИТЬ на финише фазы** — иначе ложный DEADMAN). Непрерывный
prod → каждая фаза мержится+деплоится. (2) **Человек рисует экраны в Claude Design** по
детальным промптам `docs/design-system-theming/screens-briefs-detailed.md` (#280-286, ms #39).

## Последние принятые решения

- **Дизайн-процесс (правило в CLAUDE.md)**: человек определяет вкус/арт (Claude Design +
  ChatGPT image gen), агент кодифицирует+энфорсит (барьер + Playwright-ревью). Дизайн-фаза =
  `[ЧЕЛОВЕК]`-issue без `complexity`. Пошёл через ралфа (не вручную) — это его работа.
- **Дизайн-система готова** (token-spec.md, #269): HP-модель (не очки!), темы `player`/`enemy`
  через `[data-faction]`, шрифты Pixelify Sans + JetBrains Mono (кириллица; Press Start 2P
  дисквалифицирован). Витрина #290 = мишень будущей визрегрессии (barrier не построен → вид
  ревьюим Playwright'ом ПОСЛЕ деплоя).
- **GDD** `docs/game-design/gdd.md`: HP-модель решена (переписать `score.ts`→HP до лидерборда),
  4 оружия v1, тиры сложности, биомы; PvP-realtime и наводящееся — в бэклоге.
- **Правила CLAUDE.md**: mobile-first + резиновый адаптив (390/768/1280/1920); HANDOFF всегда в main.
- Шрифты — **self-host `@font-face`** из public/fonts, НЕ next/font/google (офлайн-билд, #206).

## Следующие шаги

1. Ралф дожмёт #273→#290. Как **#290 сядет на прод**: погасить дашборд (`work:3`),
   Playwright-скриншоты `/design-system` по вьюпортам, **ревью вида вместе**, ралф правит если что.
2. Человек: экраны P0 первыми в Claude Design → экспорт zip в корень репо → я извлеку макеты,
   заведу агентские issue на React-интеграцию каждого экрана.
3. `design-consistency-gate` PRD (poupe-lint + визрегрессия) → `plan-phase` после кодификации.
4. Перед отпуском (след. неделя): свернуть VDS по `docs/deploy/vds-fold-restore.md` (floating IP
   держать, свежий образ 8638987, AAAA убрать).

## Open questions

- Бэклог: #261 (TG-канал), #262 (переносимость ралфа — паттерн не выбран), #278 (доступ к Claude
  Design из РФ-VDS — Cloudflare режет headless; работают скриншоты в VS Code).
- Ресёрч `docs/design-system-tooling-research/`: #2 Open Design, #3 РФ-доступность, #4 @theme↔DTCG, #5 вендоринг ui-ux-pro-max.
- Баги #263-265 смёржены, но визуально «не всё» — оставлено.
