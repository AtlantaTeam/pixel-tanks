# Session Handoff

**Дата**: 2026-07-24

## Текущая задача

**Дизайн-система села на прод** (PR #296, milestone «Дизайн-система и темизация из hero-арта»
закрыт): токены в `@theme`, темы через `[data-faction]`, витрина `/design-system`. Ралф-loop
завершился сам, монитор погасил себя. Сейчас — docs-hygiene и передача экранов человеку.

## Последние принятые решения

- **Доска = источник правды** (новая конвенция, в CLAUDE.md + память `board-is-source-of-truth`):
  статус/историю фаз руками в доке НЕ ведём — протухает. Правда — доска AtlantaTeam/projects/1 +
  milestones. Из CLAUDE.md убрана колонка «Прогресс» и секция «Этапы реализации».
- **Витрина `/design-system` = наш Storybook** (правило `.claude/rules/design-system-showcase.md`):
  каждый компонент `shared/ui` обязан быть на витрине со всеми вариантами/состояниями, в том же PR.
- **Ревью #296 — чистый счёт**: оба major (Button box-shadow, danger-контраст) и minor'ы (a11y
  Dialog, focus-ring, миграция реплей-экранов) ралф починил ДО мерджа. Заводить нечего.
- Детальные промпты экранов #280–286 залиты прямо в тела issue на доске (paste-ready в Claude Design).

## Следующие шаги

1. **Смёржить PR #297** (docs-hygiene: правило-витрина + rules/README-индекс + фикс дрейфа + этот HANDOFF).
2. **Решить ralph-очередь**: `.claude/ralph/ralph.config.json` → `.common.phases` указывает на уже
   закрытый milestone. Запарковать (пусто) или завести следующую фазу — см. open questions.
3. **#298** — `ShareButton` на витрине пока нет (нарушает новое правило); отдать ралфу или руками.
4. **Человек**: рисует экраны P0 в Claude Design по промптам из #280–286 → экспорт zip в корень →
   заведём агентские issue на React-интеграцию каждого экрана.

## Open questions

- **Что следующая ralph-фаза?** Варианты: (а) полировка дизайн-системы (nit'ы #296: неиспользуемые
  `--radius-*`, `--ring-focus` завязан на `--color-accent` → фокус зелёный в теме врага) + #298;
  (б) двинуться по роадмапу (шаг 6 game-next / шаг 7 auth); (в) дождаться экранов от человека.
- Перед отпуском (следующая неделя): свернуть VDS по `docs/deploy/vds-fold-restore.md` (floating IP держать).
