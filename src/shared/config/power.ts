/**
 * Единый предел силы выстрела — общий для всех слоёв. Живёт в `shared`, чтобы и
 * `features/game-engine` (танк, кламп ввода, drag-aim), и `entities/replays`
 * (кодек реплеев) читали его из одной точки: правка предела в одном месте не
 * должна молча разойтись с валидацией реплеев (FSD запрещает `entities → features`,
 * поэтому общий источник обязан лежать в `shared`).
 */
export const POWER_MIN = 1;
export const POWER_MAX = 20;
