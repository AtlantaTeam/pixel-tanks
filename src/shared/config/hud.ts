/** Поверхность HUD: полупрозрачная подложка + блюр + внешний контур в один пиксель.
 *
 *  Одна строка на всё боевое наложение — плашки, палубу, чипы и кадры витрины.
 *  До этого копий было пять (две константы в виджетах, копия на витрине и два
 *  инлайна — в `gesture-overlay` и `desktop-hover-chip`), а синхронность держали
 *  комментарии. Разъехались бы — часть HUD стала бы нечитаемой поверх светлого
 *  скина танка, причём ровно в тех местах, которые правили последними.
 *
 *  Значение подобрано под самый светлый скин: непрозрачность 0.80 — нижняя граница,
 *  при которой текст читается над песком и небом, а сцена всё ещё просвечивает. */
export const HUD_SURFACE =
    'bg-[rgba(8,12,8,0.80)] backdrop-blur-[4px] shadow-[0_0_0_1px_rgba(0,0,0,.9)]';
