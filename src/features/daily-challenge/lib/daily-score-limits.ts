/**
 * Верхний предел очков за один бой. До Auth сервер не может доверять клиенту,
 * поэтому кроме `min(0)` вводим потолок — иначе аноним отправил бы
 * `Number.MAX_SAFE_INTEGER` и возглавил лидерборд дня. Значение — общий
 * `MAX_LEADERBOARD_POINTS` (3·MAX_HP, жёсткий максимум формулы очков), один
 * источник со схемой коллекции `scores`: две одноимённые по смыслу границы не
 * разъезжаются.
 *
 * Живёт отдельно от submit-daily-score.ts: тот помечен 'use server' и может
 * экспортировать ТОЛЬКО async-функции — реэкспорт константы напрямую оттуда молча
 * обнулял все экспорты модуля и ронял next build.
 */
export { MAX_LEADERBOARD_POINTS as MAX_DAILY_POINTS } from '@/shared/config';
